spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/flower_shop
    username: postgres
    password: 1234

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  sql:
    init:
      mode: never

  h2:
    console:
      enabled: false

  cache:
    type: redis
